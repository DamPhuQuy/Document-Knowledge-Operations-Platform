services:
  frontend:
    image: node:22-alpine
    container_name: support_platform_frontend_dev
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
