# syntax=docker/dockerfile:1

# Dev: Node 22 Alpine with Vite HMR
FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . ./
EXPOSE 5173
ENV NODE_ENV=development

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
