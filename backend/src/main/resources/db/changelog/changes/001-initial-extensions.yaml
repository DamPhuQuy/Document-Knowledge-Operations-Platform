databaseChangeLog:
  - changeSet:
      id: 001-create-postgres-extensions
      author: platform
      preConditions:
        - onFail: MARK_RAN
          dbms:
            type: postgresql
      changes:
        - sql:
            sql: |
              CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
              CREATE EXTENSION IF NOT EXISTS "pgcrypto";
              CREATE EXTENSION IF NOT EXISTS "vector";
              CREATE EXTENSION IF NOT EXISTS "pg_trgm";
